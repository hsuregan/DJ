<script>
$(document).ready(function() {

 $("#album_title").autocomplete({
  source: "/autocomplete_album",
  minLength: 2,
  select: function( event, ui ) {
  //alert(ui.item.id + " " + ui.item.value);
  }
  });


  $("#artist_name").autocomplete({
  source: "/autocomplete_artist",
  minLength: 2,
  select: function( event, ui ) {
  //alert(ui.item.id + " " + ui.item.value);
  }
  });



});
</script>

<%= @albumTitles %>
<%= @artistNames %>
<%= render :partial => 'form' %> 
